# LifeIndex 产品基线

版本：3.3.0。内部包版本：`3.3.0`。本文件定义当前批准范围，实施及验证状态以 [PLAN](PLAN.md) 为准。

## 产品

LifeIndex（Index your life.）是单人、私人使用的本地优先生活索引。主要设备为 iPhone，通过 Safari 添加到主屏幕使用；核心记录支持离线。

| 入口 | 当前职责 |
| --- | --- |
| 今天 | 当日概览、常用记录入口、习惯完成状态 |
| 健康 | 体重及目标、运动记录、习惯与日历 |
| 专注 | 计时、完成保存、历史与时长统计 |
| 记账 | 收入支出、分类、日历流水、只读报表 |
| 设置 | 分类、外观、备份恢复、数据安全、关于 |

今天是聚合页面，不建立独立业务表。习惯属于健康。记账通过应用内表单确认写入；习惯和专注链接须校验、用户确认并具备原子回执。

## 架构与数据

React、TypeScript、Vite、Dexie、IndexedDB、Hash 路由组成静态 PWA。IndexedDB 是唯一业务主数据源，Service Worker 只缓存应用外壳；不使用 localStorage 存储核心记录。

数据库版本 5；备份导出 V5，导入支持 V0、V1、V2、V5。恢复先解析校验、展示预览，确认后原子替换；失败保留原数据。详见 [数据模型](docs/architecture/DATA_MODEL.md)与[备份契约](docs/architecture/BACKUP_SCHEMA.md)。

金额使用整数最小货币单位，体重使用整数克；自然日以本地日期键表示。日志不得包含金额、标题、备注、备份正文或其他个人内容。

## 体验与边界

使用 Ocean 深浅主题、简体中文界面、系统字体及可访问图标。区分查看与写入入口，失败保留输入，离开未保存表单需保护草稿。专注显示实时已用、剩余和目标时长；汇总只计已保存的完成会话。

当前不包含账号、后端、云数据库、分析追踪、实时跨设备同步、原生应用或复杂财务管理。备份可由用户存入 Files/iCloud Drive，但不代表自动云同步。

取舍依次优先数据安全、日常操作效率、离线可靠性、可维护性与视觉质量。范围变化形成 ADR，并同步计划、需求、设计与验证文档。物理 iPhone 验收必须由用户实际执行并确认；浏览器模拟不替代真机证据。
