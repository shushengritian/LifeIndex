# ADR-0021：操作体验优化持续交付与发布授权

2026-09-21。用户要求：“不用每一步都问我，新版本发布后再结束任务，然后提醒我真机验证”。

授权完成 ADR-0020 / I1–I6 已确认范围的本地开发、测试、提交、推送与 GitHub Pages 发布，替代此前“发布需另行授权”的门槛。版本定为 3.1.0；不改 V4 数据/备份，不引入后台、账户或依赖升级，不删除用户数据。保留用户独立的 AGENTS.md 改动，不纳入发布提交。

发布前执行静态、单元/集成、浏览器回归与构建，部署后验证实际线上版本和关键路径。自动化不能冒充物理 iPhone；发布完成后提供真机操作清单，由用户执行。账号/权限或安全阻碍仍须明确报告，不绕过审批或隐藏失败。
