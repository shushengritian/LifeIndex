# 专注 R1 / R1.1 实施交接

2026-09-21。按本轮用户明确授权实施；BRIEF / DESIGN 中的“设计待批准”保留为设计阶段历史。本子任务只实施专注模块，不提交、不发布，不修改全局样式、版本或 PLAN；总集成与发布由主代理处理。

## 已实现

- 准备页保留真实标题、25/50/自定义时长、分类及备注、草稿和开始写入保护。圆形播放触点64px；准备摘要与沉浸计时分离。
- 运行使用320×320 viewBox、半径148的两层完整 circle；轨道始终存在，进度从12点顺时针表达已用比例，零进度隐藏圆头残点。沿用实际时间格式，长时长继续显示小时，避免把既有90分钟等会话格式缩减为分钟。
- 事项和主读数在圆内，已用/目标在圆外。停止触点64px。长标题可在其区域滚动，页面保留纵向滚动，短屏允许滚动到停止入口。
- 收起仅改变组件显示状态，不写数据库、不暂停、不重置起止时间、不卸载自动完成hook。摘要提供重入按钮；重入/收起后焦点转移到新场景控制。重新进入路由或刷新仍读取真实active记录。
- 沉浸场景通过focus.css中条件化的`:has(.focus-immersive)`隐藏外壳标题和底导航；收起、完成、离开路由即恢复。仅在该场景调整外壳内容底部留白，不隐藏PWA错误/离线提示。
- 停止面板包含“继续专注 / 保存并结束 / 放弃本次，不保存”；放弃另有明确确认。保存中禁止退出及重复操作，手动失败仍保留原确认端点供重试。

## 真实业务与设计边界

原FocusRepository、useFocusCompletion、schema和备份未改。自然结束仍按expectedEndAt协调保存；失败固定时长、显式重试，不每秒重试。计入汇总的仍只有已完成且成功保存的记录。历史、保存描述、删除、草稿保护及取消不保留会话的语义沿用原实现。

未移植原型内存计时、演示统计或模拟保存。独立结果页不是本次新增范围：成功后沿用真实准备页及历史汇总，保存中/失败提示与重试保留。原型未展示的完整状态不能被当作已完成视觉验收。

Impeccable仅手动参考skill、Operate与craft-floor：保留Ocean、系统字体、图标可访问名称、明确破坏性文案及短屏滚动；未运行启动器、下载引擎或安装hook。

## 检查与集成

- 定点Vitest：8文件23项通过，覆盖domain、repository、开始失败/读取失败、历史/描述写入、手动结束端点复用、取消以及自然结束重试。
- 新增focus-immersive.test.tsx：0/50/100%完整圆环、收起/重入不写库、焦点目标、收起后自然结束失败与显式重试。JSDOM只证明结构和状态，不证明真实布局。
- 更新focus-confirmation / focus-completion现有断言适配三选项停止面板。
- 更新focus-progress.spec.ts：circle选择器，沉浸导航隐藏/收起恢复，环形正方比例、64px停止触点、运行到统计间距和320×568/430×932可达性。浏览器执行交由总集成，本子任务未宣称通过。
- app-shell.spec.ts由主代理独占：专注保存按钮改为“保存并结束”；运行后导航到今天前先点“收起计时，继续运行”。事项heading及“提前结束”可访问名称保留。
- 使用本地node_modules中的prettier、eslint、tsc和vitest入口，避开pnpm的依赖自动安装检查。不改变依赖。
- 本模块定点ESLint及格式检查通过；修正焦点effect后再跑新增沉浸集成测试。最后一次共享工作区tsc被范围外`src/features/health/cessation/CessationPage.tsx:185`的TS2367阻断，已通知主代理，未修改该文件。

## 风险 / 未验证

条件化外壳选择器依赖`:has`，应在总集成Chromium/WebKit检查导航恢复与滚动。真实iPhone安全区、键盘、VoiceOver、后台恢复及大字长标题尚未验证；自动化不能写为真机通过。无本子任务部署记录。
