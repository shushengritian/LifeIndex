# 阶段 5 · Ocean 开发计划与证据

## P5-07b 发布审查与并发保护

最终结果：最新源码完整重建后的 Chromium/WebKit 回归 47 通过、1 原有 WebKit 离线壳重载专项跳过（不是通过），无失败/重试；全量 45 文件/229 项通过，类型、lint、格式与 diff 检查通过。后续原生快捷指令、最终视觉/无障碍专项与部署仍不由这些结果代替。

复审补充：原 P1 在静态独立复审中关闭。随后发现并修复同 actionId 换参数时旧预览短暂复用的 P2，改为绑定 ParsedAction 身份，检查期间只显示加载；该修正也通过只读复审。新增测试均启用 StrictMode，补路由复用和失败重试，共 7 项；与既有 Action 草稿共 11 项通过。最终本批全量 45 文件/229 项、类型和全仓 lint 通过。

浏览器首轮（P2 修复前构建）46 通过、1 跳过、1 Today 跨模块测试失败；失败快照仍在健康页，表明测试在习惯提交收尾前点击导航被正确 busy 保护拒绝，又误把健康页的 1/1 当成 Today。测试现在等待编辑器关闭、断言 Today URL，并等待专注活动按钮可用后再切页；最新源码已重新构建并启动完整双引擎回归，结果待记录，不把首次失败写成通过。

独立只读审查发现 P1：check-habit/start-focus URL Action 在保存时只禁用确认，取消/导航仍可离开，异步完成会再强制跳转。ActionPage 已加入同步 ref 提交锁、先于事务登记的全局 busy、取消禁用、失败释放和按路由生命周期代次拒绝卸载后的回调导航；成功先同步释放本 token 再跳转，其他 token 不受影响。日志仅 actionType/操作/错误类型，不包含 URL、习惯、标题或金额。Impeccable 仅手动 harden 参考用于忙碌、失败与退出语义，不运行 launcher 或改视觉方向。

新增 5 个集成用例，覆盖两类动作双击、延迟真实事务/取消导航保护、失败后取消不落库及强制卸载不再跳转；与既有记账草稿 4 项合计 9 项通过。健康历史测试扩展到每种 40 条，编辑最早记录并验证失败删除/重试；4 项通过，不声称验证了 jsdom 无法测量的滚动位置。

Vitest 版本常量改为读取 package.json，增加一致性断言，避免测试日志一直显示历史 1.0.0。质量脚本加入三项快捷构建检查，两条 CI/Pages workflow 通过既有 pnpm quality 继承；不新增依赖、不签名或上传私人配置。现行门槛集中于 G5_RELEASE_AUDIT，历史记录保留。

## P5-06g 原生 R2 复核与 R3 修正

R2 导入预览显示 OCR → 替换/匹配的输入已连接，URL 编码引用也正确显示。进一步发现：空替换显示系统默认“世界”，format.date 未显示 ConfirmedDate，循环引用显示“重复结果”而非当前项目，未知类型字典比较缺少操作数。R3 将空替换/日期设为显式文本 token，字典比较先转文本，循环使用原生 Repeat Item/Repeat Item 2 变量。模型不再将循环结果假装成当前项目。

R3 共 289 动作，plist 检查与 Apple 签名成功；19 项模板模型测试和三项作者检查通过。尚未原生复核/运行 R3，已询问是否允许添加本机候选做合成测试（可能经系统 iCloud 同步）。未安装、未修改用户已有指令；目标 iOS/同账本仍待确认。

## P5-06f 完整快捷指令候选与原生预览

新增 `build-shortcut.mjs` 及 input/category/handoff 分段构建器，完整候选 278 动作。单张共享图优先，无输入截屏，多图停止；系统 OCR 后始终核对金额时间。选择本地配置 JSON，校验版本、业务域、ID、可用标记、重复 ID、父子同收支、父级存在与最多两级，再选择收支、一级和可选二级（含“不细分”）。同名分类以显示末尾稳定 ID 区分，分类名不进入 URL。为防双层遍历失控，候选暂拒绝超过 1000 项配置；此为快捷指令限制，不限制 App 分类数量。

每次运行用六段原生随机数构造符合小写 UUID v4 语法的 actionId；不是密码或授权凭据，也不声称密码学随机。它在本次草稿转交时只生成一次，App 保存重试沿用。同一截图重新运行会产生新草稿，不能当作图片内容去重。仅五字段 actionId/amount/occurredAt/categoryId/type 逐项编码进固定 LifeIndex hash URL；不带 OCR、名字、图片或订单号，打开前提示尚未记账和同账本检查。

验证：完整 Vitest 44 文件/220 项通过（当时新增 18 项），修正原生显示字段后定向 19 项通过；全仓 ESLint 和 TypeScript 通过。新增 JS 模型执行真实生成动作分支/循环，再把生成 URL 交给生产 `parseActionRoute`，验证一级/二级/收入、不细分、损坏配置、多图和非法金额。模型刻意不代表 Apple 类型转换、权限、日期解析或取消行为。

本机 plist 校验和完整 R1/R2 候选系统签名成功。Computer Use 仅打开 R1 导入预览，没有添加或运行指令，也未打开用户原有指令。预览发现替换/匹配的输入显示未绑定；R2 将文本参数改为 token string，match/split 同时提供旧 WFInput 与新 text 输入键，加入序列化回归。R2 原生复核未完成，已退出未完成导入。签名文件仅在临时目录，不复制 public。原生字典类型/布尔转换、严格配置校验的系统行为、循环连接及设备同库依旧是必验项。

下一步是 R2 原生连接与合成数据执行检查、目标 iOS 验证及安装说明，再继续最终 UI/无障碍/发布审计。不能将本批标为可安装正式交付。

## P5-06e 金额与时间核对候选

`shortcut-payment-rules.mjs` 共用合成测试和原生匹配表达式。优先完整实付字段，仅在没有该字段候选时使用独立人民币金额行；不将原价/优惠/余额标签、订单号、O/0 歧义或外币字段当作实付。规范全角数字、分隔符与换行，不推测字符错误。金额始终询问核对；完整交易时间才进入日期检测，检测失败或缺失时要求手动选择，不宣称默认当前时间来自截图。

原生确认阶段已接入输入构建器，共 63 个动作。金额默认文本使用 `WFTextTokenString`，新增 tokenText 按 UTF-16 位置生成引用并拒绝未绑定占位符。数据输入仍使用 attachment，避免混用展示与数据引用。没有记录 OCR/金额/时间，构建与检查仅打印步骤、动作数和结果。

验证：三份 Node 检查（plist、payment、confirmation）与脚本 ESLint 通过；分支模型验证候选选择、全角千分位去除、用户修正优先、空候选和日期检测失败回退。模型明确替代 Apple 日期检测，不验证其实际解析；原生编辑器连接、系统 OCR 和 iPhone 同账本尚未验证。分类配置消费、一级/二级选择、唯一 actionId、逐字段 URL 编码与端到端运行仍待完成，未开放安装或发布。

## P5-06d 原生动作构建底座

新增原生 action/变量/output/XML 构建助手及输入阶段构建脚本。无远程运行时、无第三方 App 动作；共享图片优先，否则调用 takescreenshot，再交 extracttextfromimage。以 Nothing 结束避免未完成阶段意外输出全文。构建器拒绝覆盖既有目标，日志仅动作数和步骤。合成引用/转义/元数据检查通过，系统 plutil 校验通过，生成 11 个动作；这不是端到端记账模板，待补候选规则、文件配置验证、分类选择和 URL。真实 iOS 版本仍待用户回复，未安装/执行模板，不宣称 OCR 行为已验证。

## P5-06c 原生模板签名链路

验证：项目签名包装脚本再次生成非空签名探针，退出 0；指向已有目标时退出 1 并拒绝覆盖。全仓格式、lint、diff 检查通过。产物仅在临时目录，不进入正式发布目录；没有导入/执行探针，不将其记为 OCR、模板或真机验收。

macOS 15.2 实测：最小文本 plist 在补齐元数据并使用 `.shortcut` 规范输入后，由系统 `shortcuts sign --mode anyone` 签名成功，输出 21,635 字节。此前 XML/二进制格式错误也保留为诊断事实，未将“命令存在”当成“可安装模板交付”。新增 `scripts/sign-shortcut.mjs` 负责元数据检查、规范输入、禁止覆盖和输出非空检查；仅公用模板可送 Apple 签名，私人分类不进入模板。见 [构建与验收](../operations/SHORTCUT_TEMPLATE_BUILD.md)。真实业务模板/导入编辑器核查和 iPhone 系统专项仍待完成。

## P5-07a 完整质量门槛启动

最终本批结果：完整 E2E 47 通过、1 原有 WebKit 壳离线重载专项跳过（不是通过）；涵盖本地真实 IndexedDB 编辑、分类、备份导入导出、动作去重/脱敏、离线写入、自动无障碍、320px 触控等。此前 25 个格式问题文件已机械格式化，四份原型行为脚本重跑通过；全仓格式、ESLint、类型和 diff 检查通过。本批不改变业务代码或依赖，不代表剩余快捷系统集成、整体验收或发布完成。

开始执行 Chromium/模拟 iPhone WebKit 共 48 个既有 E2E。首次启动被 pnpm 隐式依赖检查和沙箱端口限制阻断；改为调用已安装 Vite 预览二进制，并通过授权启动本地服务器，不重装/升级依赖。首轮 40 通过、5 失败、1 跳过、2 未运行；五个失败来自旧 Today 标题位置、记账入口模糊匹配和分类编辑已改 Sheet 的测试路径。按现行交互更新后，两浏览器对应 6 项定向测试全部通过，完整重跑进行中。WebKit 壳缓存重载是原有明确跳过项，不记为通过。

全仓 ESLint 补齐原型浏览器环境和复核脚本 CommonJS 环境；G4 多个 classic script 共享全局，按 AST 真实顶层声明建立检查边界，仍检查未定义名字与函数内部无用变量。不降低生产 TS/React 规则，修正两个无效初始赋值后全仓 lint 通过。全仓格式检查暴露早期原型/审查脚本格式未统一，仍待处理，不宣称全部门槛已过。

快捷指令环境：本机 `shortcuts sign --help` 确认可签名已有文件（anyone / people-who-know-me）；这仅证明工具存在，不代表模板可安装或 iPhone 可用。Apple 官方说明见 https://support.apple.com/en-md/guide/shortcuts-mac/-apd455c82f02/mac 。

## P5-06b 快捷草稿修正

验证：全量 43 文件/202 测试通过，类型、相关 ESLint、构建和 diff 检查通过。新增失效分类补选/写入失败重试/同链接去重、导航与取消确认、原生时间值提交、pending 写入防重与导航拦截集成测试。E2E 与线上 smoke 的金额核对/取消选择器已同步，但本批未执行；快捷页视觉、原生顶层对话框和 iPhone 专项仍需最终门槛验证。

新增 TransactionActionEditor：金额/收支/时间/备注可核对修改，使用正式 FinanceCategoryPicker；不存在/归档/跨收支分类只提示补选，不丢金额和时间。ActionService.inspect 的修复模式只影响预览，默认严格行为和 execute 原子校验不变。保存沿用原 actionId，失败保留草稿并可重试，成功或已处理后替换掉敏感 URL。重复打开请求读取 receipt，不再写入记录。

草稿和 busy 单独登记 PWA guard；取消需应用内确认。成功/确认取消先同步释放本草稿 guard，再导航，避免自己拦截成功跳转；其他草稿保护不解除。保存有 ref 防重、字段整体禁用。时间从原生输入当前值读取；未修改的输入保留收到的原始瞬间（秒/偏移语义），修改后按设备本地时间生成日期键与偏移。快捷分类配置需要更新、Safari/PWA 可能隔离均有明确提示。不声称完成快捷指令系统调用。

## P5-06a 快捷记账分类配置

验证：全量 42 文件/198 测试通过；类型、相关 ESLint、构建和 diff 检查通过。新增配置投影/排序/不可变性、归档继承、无有效分类、错误层级、系统分享取消和下载异常清理单测，以及设置读取最新分类/导出失败重试集成测试。本批未执行真实系统分享、浏览器下载或 iPhone 快捷指令验证，不能将 mock 文件交接视为实际文件已保存。

`shortcutConfig.ts` 构建独立 V1 配置，先验证分类记录与层级再筛选 finance 可用项，父级先于子级，字段采用白名单投影。配置不含图标、时间戳、账目、金额或设置；有效分类为空时拒绝导出，父级归档会排除子级。SettingsPage 在导出时重新读取真实分类，使用已有同步锁与导航保护；支持系统分享或浏览器下载，取消/失败不写账本。UI 告知 iCloud 文件同步分类名称的隐私边界与分类变更后重新导出要求。

安装模板、配置消费校验、Action 修正流程及 Safari/PWA 真机同账本检查继续，不宣称指令安装或端到端流程已完成。

## P5-05c 设置入口与详情

按已验收原型落实四组长条入口，分类默认折叠并使用既有彩色图标；其他组增加戒烟计划入口，可进入既有入口管理。外观、导出、恢复、关于和快捷记账说明使用 `/settings/:section` 独立页面，不在首页展开所有表单。详情支持返回设置，外观以示意色块、说明及 SVG 对勾表示选择；保留 P5-05b 全部写入/恢复保护。快捷记账说明明确 OCR→核对→分类→App 确认流程以及 Safari/PWA 边界；模板/配置尚未接入，不能误称安装已交付。

验证：41 文件/192 测试通过；TypeScript、相关 ESLint、生产构建和 diff 检查通过。新增五个详情的入口/返回测试，既有设置失败路径调整到真实路由。浏览器验证首页/外观两主题各 320/390/430 宽度无横向溢出；[截图与范围](../design/reviews/p5-05c/README.md)。E2E 和 deployed smoke 选择器同步，尚未运行，真机不作通过声明。

## P5-05b 设置安全交互

验证：全量 41 文件/191 测试通过，TypeScript、相关 ESLint、生产构建及 diff 检查通过。新增 5 项覆盖取消/失败重试、重复恢复与 busy 退出保护、恢复后外观读取故障、主题回滚重试、导出元数据故障。ConfirmDialog 的原生顶层行为以 jsdom shim 建模，本批不宣称浏览器、E2E 或物理 iPhone 验证；构建仍有既有 inlineDynamicImports 废弃警告。

恢复预览之后使用应用内 ConfirmDialog，取消回预览、失败留在确认框重试，写入期间禁用取消。外观/导出/文件检查/恢复共享同步 ref 锁并登记全局 busy，避免重复操作或主题写入与恢复并发。外观保存失败回滚显示并释放锁。

恢复事务成功后即关闭预览；后续外观读取失败只提示外观未同步，不再错误宣称数据回滚。导出文件交接和时间元数据保存同样分开处理；系统接收文件不等于已确认设备文件存在。预览警告统一强调备份缺失记录不会保留，不将所有旧版本误称为不含戒烟数据。日志不包含文件名、内容或记录信息。新增设置故障集成测试并同步 E2E 确认选择器；本批 E2E 未执行。设置详情分层/快捷入口另批继续。

## P5-05a 今天页

验证：40 文件/186 测试与构建通过；浏览器直达记账/取消路径通过。完整主题/宽度视觉矩阵仍待最终门槛验证。

落实已验收行动优先首页：日期置于标题后，主入口“记一笔”直达 `/finance/new` 复用记账表单；专注入口按真实加载/失败/活动/待保存状态显示，不将读取失败当成未开始。习惯仍是今日计划，增加同步防重与全局 busy，失败保留记录。摘要精简为今日支出/收支笔数、完成专注时长/次数，空账目显示尚无记录，模块故障继续隔离。历史与完整报表仍在各自模块。无需新增业务数据结构。

## P5-04k 戒烟确认与命令保护

验证：全量 40 文件/185 测试通过，类型/相关 lint/构建通过。保留并更新既有吸烟草稿重试同 ID 与 Escape 用例，新增删除/结束失败重试及顶层确认 Escape 隔离测试。四类表单通过 disabled fieldset 锁定全部输入。全量运行曾发现导航日志 effect 时序断言偶发早于 effect，改为 waitFor，不放宽最终期望；本批未重跑浏览器或 E2E，完整视觉/真机不作通过声明。

删除事件/结束计划使用 ConfirmDialog，失败保留确认框和原数据；CessationPage mutate 增加 ref 锁及全局 busy 导航保护。共用 useDraftActions 改为应用内放弃确认和同步保存锁，Escape 遇顶层确认时不穿透关闭底层表单；计划/吸烟/烟瘾/原因均接入。E2E 确认路径同步修改；外部 WHO 网页的离站确认仍保留现有行为。仍需戒烟整体视觉和最终回归。

## P5-04j 健康首页与体重趋势

验证：39 文件/182 测试通过，类型/相关 lint/构建通过；浏览器两主题三宽度无 document 溢出，收紧体重面板并合并页脚。[证据与未覆盖项](../design/reviews/p5-04j/README.md)。

体重保留单个 Ocean 渐变面板，运动/戒烟/习惯采用彩色图标与轻量分隔区。WeightTrendChart 使用真实最近 30 个本地日每日末次测量，UTC 日序仅用于横轴等距计算（避免 DST 长短日），不改变记录时区。缺日不补值，连线标注仅示趋势；单点不画连线、空窗口明确说明可到历史查看。纵轴显示公斤刻度，并留至少 0.5 kg 上下边距避免放大小波动。日志只有点数，不输出日期、体重或图形坐标。新增纯视图单测覆盖去重、时间窗口、缺值/单点与输入不变。无 schema 变化。

## P5-04i 习惯管理与统计分层

验证：38 文件/180 测试通过，类型/相关 lint/构建通过；浏览器新增/统计和浅色三宽度检查通过，移除统计嵌套卡片。[证据与未覆盖项](../design/reviews/p5-04i/README.md)。

健康首页保留今日打卡与“管理习惯与统计”入口，独立 `/health/habits` 显示全部习惯。新增/编辑改用 Sheet，受控字段整体在写入中禁用，ref 防重、失败保留、放弃草稿确认。统计沿用现有连续天数/14 周热力图/本月完成率算法，移入独立 Sheet。打卡/撤销共用写锁，暂停恢复需确认，失败保持确认和历史。未新增打卡规则或数据库字段；完整布局和跨模块回归仍继续。

## P5-04h 健康历史记录布局

验证：37 文件/179 测试通过，类型/相关 lint/构建通过；浏览器确认整行编辑与取消删除，修正日期分组样式覆盖，浅色三宽度无溢出。[证据和未覆盖项](../design/reviews/p5-04h/README.md)。

新增 HealthHistoryList：按仓储排序的本地日期连续分组，以日期标题、彩色 SVG、时间和记录摘要呈现，整行可打开编辑；列表不再放零散编辑/删除文字按钮。删除移入 WeightForm/ActivityForm，仍调用共用确认流程，成功后关闭编辑，失败保留记录与表单。新增日期分组/整行点击单测，并同步历史集成/E2E 操作路径。健康主页视觉、习惯/戒烟与完整回归仍继续，不宣布整版完成。

## P5-04g 体重目标保护

验证：全量 36 文件/178 项通过，类型、相关 ESLint、构建及 diff 检查通过；本批确认流程由集成测试覆盖，未另行声称浏览器/真机通过。构建保留既有 inlineDynamicImports 废弃警告。

TargetForm 补齐草稿取消确认和清除确认；保存/清除共享 ref 锁与输入禁用，失败保留输入/原目标，可在确认内重试清除。日志覆盖校验、入口、成功和失败但不包含目标值。同步扩展健康集成测试（清除失败重试、放弃目标修改）及 E2E 确认路径。仍未完成完整健康视觉与其他模块，不发布。

## P5-04f 健康草稿与写入

验证：全量 36 文件/177 项通过，TypeScript、相关 ESLint、构建和 diff 检查通过；构建保留原有废弃警告。原生日期 change 延迟由 DOM 故障模型覆盖，不等于物理 iPhone 验证；本批未重跑浏览器/E2E。

体重/运动取消的 window.confirm 替换为共享 ConfirmDialog；继续填写保持草稿，明确放弃才关闭。保存使用同步 ref 锁并禁用所有数据控件，失败释放写锁但保留输入，日志仅含操作/失败类别。从 FormData 读取原生 datetime-local 的当前值，处理最终 change 事件延迟。新增两类表单集成测试覆盖确认、禁用、防重、失败重试、原生日期提交和明确放弃；不修改 schema。

## P5-04e 健康完整历史

验证：全量 36 文件/175 项通过，类型、相关 ESLint、构建及 diff 检查通过；浏览器确认健康→体重历史→返回→运动历史路由和空态。7 条完整数据、未来日期编辑和删除失败重试由集成测试覆盖，本批未执行 E2E 或真机测试。构建保留既有废弃警告。

新增 `/health/weight-history` 与 `/health/activity-history`，由 HealthPage 受控视图复用相同 IndexedDB 查询、创建和编辑流程。健康首页保留摘要与入口，不再嵌入截断的 3/4 条明细；历史展示全部查询记录，体重范围扩展为全日期，允许修正未来日期误填，30 天趋势仍按现有本地窗口排除未来值。独立页不渲染无关习惯/戒烟。删除改用 ConfirmDialog，ref 防重复写，失败保留确认/记录并允许重试，busy 阻止导航。

新增两类历史集成测试：各 7 条记录、未来日期编辑入口、首页无明细、删除失败保留后重试；同步修改健康 UI 与 E2E 路径。日期分组、彩色记录行、删除移入详情及草稿确认/健康视觉仍待后续，不宣称 P5-04 全部完成。

## P5-04d 专注 Ocean 首页

验证：全量 35 文件/173 项测试通过，类型、相关 lint、构建通过；首页两主题三宽度无横向溢出。[证据与范围](../design/reviews/p5-04d/README.md)。

将已验收 Ocean 弧线路径落实为 FocusStage，弧线只展示真实时间差进度，不承担持久化计时。超过一小时展示 HH:MM:SS，长时钟缩小字号；预览限定在 0–240 分钟显示范围，提交仍校验真实输入。首页移除旧环形卡片，分类/备注收进默认折叠 details，移除首页自动聚焦以避免打开就拉起键盘；开始时同步 ref 锁与字段禁用，失败保留草稿。历史接入分类真实 SVG/颜色，汇总去掉多余卡片。E2E 分类选择补充展开步骤。尚待整体视觉回归，不代表健康或全部阶段 5 完成。

## P5-04c 专注详情与命令确认

详情改为 Sheet，开始/结束时间和实际时长只读，仅编辑标题/分类/备注；已归档分类只能保留原引用。取消草稿需确认，删除移到详情内部并二次确认。保存/删除共用同步 ref 锁，失败保留草稿与原记录；删除失败在确认框内重试。提前结束/取消移除 window.confirm，使用共享 ConfirmDialog；首次确认时固定提前结束 endpoint，失败后重试复用该值。手动操作待重试时暂停本页自然完成，返回计时明确放弃待执行操作并恢复原计划；离开页面由草稿导航保护提示，不声称跨标签页全局锁。

新增专注确认集成测试覆盖放弃/继续编辑、删除失败重试、提前结束防重/固定 endpoint、取消计时确认。完整视觉和健康模块仍继续，未发布。

验证：全量 34 文件/172 项测试通过，类型/相关 lint/格式/构建通过；浏览器验证结束确认→历史→详情及草稿放弃，深色三宽度无溢出。[证据与限制](../design/reviews/p5-04c/README.md)。

## P5-04b 专注独立历史

验证：全量 33 文件/168 项 Vitest 通过，TypeScript、相关 ESLint、Vite/PWA 构建与 diff 空白检查通过。正式本地应用浏览器验证首页→历史→本周筛选→返回，空态 320/390/430 宽度均无横向溢出。记录与统计筛选由集成测试覆盖；未执行本批 E2E、完整双主题视觉或真机验证。构建保留既有 inlineDynamicImports 废弃警告。

新增 `/focus/history` 正式路由，首页只显示计时、今日/本周汇总与历史入口；路由切换继续受既有草稿导航保护。历史提供全部/今天/本周/本月筛选，分类汇总和记录使用同一本地日期窗口；秒级时长不再舍入成整分钟。新增集成测试覆盖首页隐藏明细、四种筛选、分类汇总一致性和秒级显示；已有描述写入保护测试迁到历史页，E2E 路径同步调整。详情 Sheet、危险操作确认和最终 Ocean 视觉仍待后续，本增量不等于专注全闭环完成。

## P5-04a 专注到点恢复

核对 C1 原型发现正式 Focus/Today 到点 reconcile 为未捕获 Promise，失败无用户恢复入口。新增 useFocusCompletion：按真实时钟更新，活动 ID 只自动尝试一次，ref 防并发；失败显示“时长已固定”及重试，已持久化 active 行保留。调用 repository 原有 reconcileActive，以 expectedEndAt 和 plannedDurationSeconds 固定结果。写入时登记 busy，失败不每秒自动重试；Focus 到点禁用提前结束/取消，Today 同样显示重试入口。导航离开仍可恢复本地活动记录，不引入新 schema。

新增双页集成测试：注入首次失败，确认原 active 行保持，重试成功只有一条 completed、原定结束时间和 60 秒时长不变。仅自动化故障注入，不宣称物理 iOS 后台或完整 UI 已通过。Impeccable 手动规范影响失败反馈/状态设计，无引擎运行。

P5-04a 验证：32 文件/167 项测试、TypeScript、相关 ESLint、Vite/PWA 构建通过；保留既有构建废弃警告。

下一工作仍是 C1 独立历史与完整专注视觉、健康/习惯/戒烟及今天/设置重设计，之后快捷指令、完整回归和部署；不得把本增量当作 P5-04 完成。

最新授权：用户已明确要求剩余计划全部完成并部署上线，当前长期目标保持 active。可在完整质量门槛通过后提交/推送并发布，不再重复请求一般发布授权。不得将模拟器结果当成真机；需要用户完成的设备动作仍如实登记。

## P5-03a 分类数据底座

最新增量 P5-03c：正式记账使用 `FinanceCategoryPicker` 两级图标网格，“不细分”保存根级 ID，二级只保存自身 ID；已归档历史显示完整路径且允许保留原值，不能改选不可用父级。记录行同样显示“一级 / 二级”。[本批验证](../design/reviews/p5-03c/README.md)。下方“旧下拉”是 b 批历史。分类完整故障/视觉回归继续在候选门槛覆盖，不据此宣称其余 P5-04–07 完成。

最新增量 P5-03b：[分类管理与图标验证](../design/reviews/p5-03b/README.md)。已落 42 图标、8 分组、旧值兼容、一级进入二级列表、新增编辑名字/图标/颜色、同层排序/归档恢复。当前记账表单仍为旧下拉，下一步替换为已验收的两级网格，完善完整标签/历史引用状态。未标记 P5-03 全部完成。

已实现可选 parentId、最大两层/同收支父级校验、父级归档有效性、历史引用保留、根级汇总不重复。数据库 V4 仅增加 categories.parentId 索引，不改旧行；备份输出 V4，V0–V3 严格验证后迁移。恢复继续使用全库原子事务。修复交易更新的运行时身份字段覆盖边界。新增 category-hierarchy 集成用例，线上烟雾测试期望原生 IndexedDB 版本 40，但尚未对线上执行。

后续紧接分类管理钻取页面、42 图标注册和记账两级选择；当前仅数据底座，不标记 P5-03 完成。不能发布 V4 后直接回退只支持 V3 的代码；回退以向前修复优先。

本批验证：全量 29 文件/158 项通过，TypeScript、改动文件 ESLint、Vite/PWA 构建、git diff --check 通过；构建保留既有 inlineDynamicImports 废弃警告。新增 5 项层级集成测试，含 V3 原样升级、非法父级拒绝、父归档/历史引用/Action 限制、V4 往返与旧 V3 严格边界、根级汇总。既有 V1/V2 数据升级和旧备份转换/恢复回滚测试一并通过；未进行线上发布或真机验证。

2026-09-19 用户授权“开始第5阶段”，并要求继续。仅本地实施与测试，不含推送或发布。分支 `codex/g5-ocean-implementation`；不提交或覆盖既有设计改动及 AGENTS.md 改动。版本仍 2.1.1，候选阶段再确定版本。

依据：[HLD 增量](../architecture/REDESIGN_HLD.md)、[LLD](../architecture/REDESIGN_LLD.md)、[已验收 G4](../design/G4_REVIEW.md)、[快捷指令方案](../design/SHORTCUT_SCREENSHOT_SPEC.md)。

| 批次 | 任务与验收 | 状态 |
| --- | --- | --- |
| P5-01 | 主题语义变量、导航顺序、设置齿轮，导航/主题测试；无数据变化 | 已完成本批实现与针对性验证；整版验证未完成 |
| P5-02 | 公共表单/确认/草稿退出；记账日历与新增编辑删除，失败保留 | P5-02a 应用内确认/写入锁/Tab 边界完成；整批仍在进行 |
| P5-03 | 二级分类、42 图标真实注册、备份 V4/迁移与管理页面；历史引用完整 | a–c 已实现并针对性验证，完整故障/视觉回归待候选门槛 |
| P5-04 | 健康/习惯/戒烟摘要与完整历史、专注闭环 | 进行中：专注到点恢复及独立历史已实现，其余继续 |
| P5-05 | 今天聚合与设置四组、外观/数据安全状态 | 待实施 |
| P5-06 | 分类配置导出、截图 OCR 模板、Action 修正与安装文档 | 待实施，依赖 P5-03，真机联调 |
| P5-07 | 全量回归、旧数据升级、视觉/无障碍、失败注入，候选报告 | 待实施，不等于发布授权 |

每批先更新契约→小步实现→最窄检查→功能/视觉回归→记录实际结果。禁止复制原型内存数据到真实数据库，禁止用清空数据解决升级失败，禁止把未执行检查写成通过。P5-02 与 P5-03 完成后记账才可称完整落地。

## 第一批验证记录

- `vitest run tests/unit/app.test.tsx tests/unit/settings.test.ts`：2 文件、5 用例通过。
- 全量 Vitest 24 文件/140 用例通过；类型检查、修改 TS/TSX lint、四文件格式检查、Vite/PWA 构建通过。构建工具有 inlineDynamicImports 废弃警告，未更改依赖。
- 正式应用五主页两主题三宽度共 30 组无横向溢出，导航与主题值检查通过。截图及未覆盖项目见 [P5-01 证据](../design/reviews/p5-01/README.md)。非真机，未完成 E2E/完整候选质量门禁。

当前下一任务为 P5-02：公共表单/确认与记账布局。P5-01 不代表所有模块已按原型实现，暂不向用户宣称正式版本可交付。

## P5-02a 验证记录

新增 `ConfirmDialog` 并替换记账放弃/删除的 window.confirm；保存锁定输入与退出，删除失败保留重试。Sheet 增加 Tab 边界。143 项测试/25 文件通过，新增 3 项故障/取消测试；类型、相关文件 lint 通过。浏览器验证 Escape 返回、Tab 首尾循环、新增/取消删除/确认删除，以及确认框两主题三宽度无溢出。详见 [P5-02a](../design/reviews/p5-02a/README.md)。数据库仍 V3，无迁移、发布。

下一步继续 P5-02 的记账记录行、表单布局与跨日期保存反馈，再进入 P5-03。未声称第二批或整版完成。

## 阶段 5 退出门槛

### P5-02d 跨模块写入保护（2026-09-19）

将现有 saving/working 接入 useDirtyForm 第二参数：体重、运动、体重目标（含清除）、习惯、戒烟各表单、开始专注以及设置备份读取/导出/恢复。未修改字段的提交也受保护；操作失败后释放 busy，dirty 输入继续保留。体重/运动/目标及习惯保存期间禁用取消。

专注历史描述此前没有保存状态；本批加入 busy、禁止重复提交、锁定输入和取消、失败提示与可重试，记录隐私安全的开始/成功/失败事件。未改变专注时长和数据库格式。

新增自动测试：多表单 token 独立清理、未修改的戒烟计划写入及失败解锁、真实专注描述延迟失败/保留/重试与时长不变。Impeccable 手动参考用于并发状态与失败恢复检查，无引擎或依赖变更。

验证结果：全量 Vitest 28 文件/153 项通过，TypeScript、改动文件 ESLint、Vite/PWA 构建通过。构建仍提示既有 inlineDynamicImports 废弃警告。测试为 jsdom/fake IndexedDB 自动化，不等同于真机或浏览器视觉验证。

边界：本批不是全模块防误操作完成。分类管理、外观持久化、专注结束/删除等页面级命令未统一登记 busy；历史列表切换编辑及所有表单输入锁还需结合 P5-04/05 整体重构验证。没有新增自动草稿持久化，没有执行真机或完整浏览器 E2E。后续继续 P5-03 分类与迁移，完整视觉表单和应用内确认按模块落地。

### P5-02c 导航保护（2026-09-19）

已接入草稿站内导航确认、记账写入导航锁、beforeunload 提示请求及更新命令保护。路由维持 hash URL，改用 data router 以拦截返回；见 [ADR-0013](../adr/0013-draft-navigation-guard.md)。全量 150 项测试/26 文件、类型、相关 lint 与构建通过，真实浏览器保留/放弃路径通过；[详细证据与边界](../design/reviews/p5-02c/README.md)。Impeccable 仅手动参考退出反馈与焦点规范，未运行引擎。

后续：补齐其余模块 busy 状态接入和表单视觉迁移；推进 P5-03 二级分类、图标与 V4 迁移。P5-02 整批尚未完成；下文 P5-02b 的导航待办由本记录更新。

最新进展 P5-02b：紧凑可点击账目行、图标删除、表单底部操作区、跨日期保存保留上下文与查看记录已实现。日期控件改为读取当前提交值，并增加延迟 change 回归。详见 [P5-02b 证据](../design/reviews/p5-02b/README.md)。完整分类图标/二级分类仍在 P5-03；站内导航草稿保护和其余表单视觉迁移仍待完成，不把本批作为 P5-02 全部通过。

需求到源码/测试映射完整；候选完整质量检查通过；旧数据库与备份兼容性有独立证据；未完成真机项明确列出由用户决定，不继承旧豁免。生成 G5 报告后申请本次发布许可，不擅自 push、发布或建立自动化任务。
# P5-07c 健康长列表浏览器回归

新增 `tests/e2e/health-history-scroll.spec.ts`：隔离浏览器数据库写入每类 40 条合成记录，刷新装载（原生 IndexedDB 测试写入不会通知当前 Dexie live query），滚动至最早记录并编辑、保存、重开核对、取消。断言记录仍在视口、保存前后页面滚动差小于 5px、40 条列表完整。测试日志只包含类型/数量，不包含记录内容。

Chromium 与移动 WebKit 共 4 项通过。没有修改产品运行代码；不把模拟设备视为物理 iPhone，也未覆盖跨路由返回的滚动恢复。完整旧套件结果仍为上一批 47 通过/1 专项跳过，本批四项是独立运行，不虚报为全套重跑。
# P5-07e：最终自动无障碍矩阵与焦点修复

新增 accessibility-matrix E2E：15 个主页/独立子页 × 深浅主题 × Chromium/移动 WebKit，在 320px 下检查横向溢出与 axe；这只是初始/空状态自动扫描，不代表所有内容、读屏或原生 200% 放大通过。

键盘回归覆盖记账 Sheet 首次焦点、Tab/Shift+Tab 边界、确认框默认安全按钮、Escape 返回表单按钮，以及放弃全部草稿后返回新增入口。首次两引擎均发现确认框卸载 fallback 覆盖 Sheet 恢复焦点，已修正 ConfirmDialog，使已有有效焦点优先于 main 回退。遵循既有设计，仅修复交互，不调整视觉方案。
# P5-07f：完整回归与迁移证据补强

最新焦点修复后的完整回归：45 个 Vitest 文件、229 项通过；完整浏览器 57 通过、1 原有 WebKit 离线壳重载专项跳过。随后加强 category-hierarchy 的 V3→V4 用例：旧测试仅账目表非空，现为全部 12 表提供关联一致的合成记录，通过 initialize 实际启动与幂等种子路径验证逐表完全保留，并断言 parentId 索引建立。该文件 5 项重跑通过，无运行时存储代码变更。

本轮源码复核检查了 V4 增量索引、旧备份逐版本验证与导入引用完整性、分类写事务及隐私日志白名单；没有发现新的存储实现缺陷。这是主执行者复核，不将其记为另一位审查者的独立批准。浏览器模拟不替代原生快捷指令或 iPhone 的待验收项。
# P5-07h：习惯热力图交互补齐

原型 g4-app 支持点日期查看状态，生产旧实现只有不可交互 span。现为按钮、状态播报、完成符号/虚线/选中轮廓，保留只读语义与原 98 格统计。局部横向滚动确保触点至少 44px，并默认展示当前周。

habit-ui 集成补测历史日期/今日完成状态，点击前后数据库仍只有原始一条打卡；浏览器新增双主题、双引擎、320px、键盘 Enter、自动无障碍与页面无横向溢出检查。没有改变打卡业务规则、存储 schema 或原型批准范围。
# P5-07i：习惯列表与详情操作层级

完整管理页改为彩色 SVG 图标整行入口，显示名称/计划/暂停状态；移除每行三个文字操作以及管理页重复的今日列表。健康首页仍保留今日打卡。点击列表进入现有受保护的统计 Sheet，在其中集中编辑、暂停/恢复与今日完成/撤销，保留统计和热力图。写入期间详情操作与关闭按钮禁用。

这是批准的列表→详情层级在阶段 5 既有 Sheet 容器中的实现，不新增数据模型。集成用例按真实新路径复核查看、暂停失败重试、恢复、编辑草稿保留；热力图浏览器用例改为从详情入口打卡与重载后再次打开详情，不依赖被删除的列表文字按钮。
# P5-07j：戒烟入口与历史详情

P5-07o 返回来源补充：按 G2 S01/J02 修复 Today 快捷记账保存/取消后停留记账的问题。FinanceNewPage 在表单卸载和导航锁清除后返回 Today，Today 显示成功状态；既有手工点击 Today 的 E2E 改为要求自动返回，新增干净取消、草稿继续填写/明确放弃、零写入及刷新无重开的回归。两引擎相关 6 项与全量 234 项 Vitest、类型、相关 lint 和构建通过。未改变 schema，尚未部署。Impeccable 仅手动参考退出及状态保护，不运行启动器或改变视觉方案。

同轮实页核对发现改变计划原生日期后保存仍落初始时间。PlanForm/SmokingForm 提交改为禁用字段前同步捕获 FormData 的 startAt/occurredAt，并同步回 React 状态供失败重试；日志不含时间值。新增两类控件未派发 change 的测试，验证实际提交时间及失败后保留。原截图中的 0 天不能作为日期正确的证据，修复后另行验证。

对照批准的 g4-cessation：日快照作为唯一主动作，下方明确只代表当前时刻；吸烟/烟瘾为并列次级图标入口，替换原分散的“我想抽烟”与文字记录链接。历史事件改为整行进入编辑，不在列表暴露删除；详情中删除仍由同步锁和 ConfirmDialog 保护，事务成功关闭详情，失败保留记录与详情供重试。空态以叶片图标和开始入口呈现，不引入医学承诺。

4 项 cessation-ui 集成、8 项双引擎 E2E 通过，含删除失败重试、结束/未来计划、隐藏恢复、烟瘾与离线、320/390 双主题控件及 axe。类型/相关 lint/构建通过，截图见 design/reviews/p5-07j。本批没有上线。
