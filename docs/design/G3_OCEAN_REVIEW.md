# G3 R4 海蓝样板：审稿与验证

2026-09-18。状态：独立审查所列三项已复核解决，待用户视觉审核。仅设计，不是发布候选。

## 预览与边界

[双主题三模块审稿页](prototypes/g3-ocean.html)：深色在前、浅色在后，六个窗口的内存互相独立。“单屏预览”可打开各手机页面。审稿页顶部“下一次保存模拟失败”对每个内嵌窗口分别生效一次，不影响单屏。

本稿使用合成样本，以 2026-09-18 为演示当天。只加载本机静态 HTML/CSS/JS，无真实业务存储、外部遥测、服务工作线程、第三方素材或字体。刷新重置；不修改 src、依赖、数据库或线上版本。Impeccable 仅手动辅助设计规范与独立审查，未运行引擎。

## 视觉规范（提案）

| 语义 | 深色 | 浅色 |
| --- | --- | --- |
| 页面 | `#101f35` | `#f1f6fb` |
| 表面 | `#1a2d45` | `#ffffff` |
| 抬高表面 | `#243b55` | `#e5eef7` |
| 主文字 | `#edf4ff` | `#172c46` |
| 次文字 | `#a9bdd4` | `#516982` |
| 主动作 / 曲线 | `#71d5ff` | `#126590` |
| 主动作文字 | `#10243a` | `#ffffff` |
| 分类橙 / 紫 / 青绿 | `#ffba7a` / `#c7b4ff` / `#7edbca` | `#a85820` / `#7352b4` / `#197b70` |

中文系统字体；页标题 28px，区块 17px，正文 15–16px，月汇总 15px，计时 64px；≤350px 时月汇总 13px、计时 58px。金额使用等宽数字。9–11px 用于刻度、日历金额索引、导航和次辅助信息；日历提供完整金额可访问名称与明细，G4 仍需大字专项检查。完整提案见 [实际设计变量与组件](G3_OCEAN_TOKENS.md)。

主按钮至少 50px，图标按钮 44px；原创 SVG 为统一 24px 画布、1.7px 圆端笔画，分类文字保留。五导航标签常显。模态使用原生 dialog 的焦点隔离，自定义外观，不使用 confirm/prompt。

曲线连接真实合成点位，不画虚构波形；体重有单位与纵轴范围。专注弧线对应时间比例，无暂停。表单 200ms 入场，减少动态效果时禁用。截图为本原型的浏览器渲染，不是 AI 图或第三方截图。

## 本轮验证

### 2026-09-19 细节修订

- 按用户反馈删除记账页重复的“跳转日期”控件、事件和样式；日历选日、月份箭头、新增表单中的日期字段继续保留。跨日期保存后的“查看记录”反馈不是浏览日期选择器，本次未删除。
- 设置图标原轮廓不对称，改为以 (12,12) 为中心的八齿对称 SVG，沿用 23px 显示尺寸与 1.7px 圆角描边。
- 浏览器 390×844 深浅主题截图已查看；选 9 月 17 日后明细标题与新增默认日期均同步；下个月/上个月可用，浏览器警告/错误检查为空。脚本语法与 diff 检查通过。未做真机验证。
- 本次按 Impeccable 手动 polish 流程限定修复局部缺陷，保持既定视觉方向；未调用引擎、未改生产代码、未发布。

| 检查 | 结果 |
| --- | --- |
| 脚本语法 / 格式 | Node `--check`、`git diff --check` 通过 |
| 双主题关键页 | 6 张 390×844 截图逐一查看，无空白或错误主题 |
| 320 / 390 / 430 | 三模块、双主题均无文档级横向溢出；320/430 另检查 main 无横向溢出 |
| 金额校验 | 空值拒绝；修正后可提交 |
| 脏输入退出 | 填写金额/分类后关闭，需确认；继续编辑保留输入 |
| 保存投影 | 新增 28.50，月支出 1162.00 → 1190.50，结余 8638.00 → 8609.50 |
| 失败与重试 | 18.50 失败后保留输入，重试后支出 1180.50，未重复追加 |
| 运动新增 | 3 次 / 90 分钟 → 新增 20 分钟 → 4 次 / 110 分钟 |
| 专注开始 / 取消 | 输入事项后开始；取消有确认，返回可开始，不增加记录 |

颜色对比度（sRGB，仅所列颜色对）：深色次文字/表面 7.25:1、按钮 9.48:1；浅色次文字/页面 5.23:1、按钮 6.38:1；输入边界/底色深 4.63:1、浅 3.26:1。不代表完整 WCAG 审计。

## 独立审查

独立只读审查查看六张截图与原型源码，认可蓝色层次、统一图标、首屏结构；初次 verdict 为 FIX，要求修正自定义时长显示不同步、健康备注丢失、改日期保存后缺少回看入口。

修正策略：输入时更新计时、弧线标签和选中态且保持焦点；健康记录保留备注并在历史显示；跨日期保存保留原浏览位置，增加持久的“查看记录”入口。第三项遵循 G2 返回上下文约定，未采用直接跳走的建议。

复核结论：三项均 resolved，可交用户审稿，仅限本次修复清单。主代理浏览器验证 40 分钟同步显示且输入保持焦点、64.6 kg 健康备注可回看、跨日期保存 16 后保留原日期并可主动查看 10 月 2 日记录。独立审查亲自检查源码和三张修正截图，未自行运行浏览器；不将此结论视为生产发布或 G4 全量验收。截图说明见 [证据目录](reviews/ocean-r4/README.md)。

## 已披露限制与下一步

- 今天、设置、完整编辑/删除、目标设置、戒烟全流程、全量空/加载/读取失败状态留给 G4；本稿入口明确说明范围。
- 表单为关键字段样本，未展开专注可选分类/备注等全部字段；不替代生产 LLD。
- 体重/运动详情仅浏览当前内存集合；没有验证真实数据库失败、后台恢复、iPhone 键盘/安全区、主屏幕或 200% 放大。
- 内嵌 dialog 的自动定位点击存在工具滚动限制，失败重试使用截图定位点击验证；单屏编辑正常。不把工具限制写成产品缺陷。
- 无生产构建或产品回归，无真实 iPhone 验证，无发布操作。

请审核色调、三模块统一性、曲线与图标、主次动作和短表单。G3 通过后只进入 G4 全量高保真原型；不自动授予生产实现或发布权限。
