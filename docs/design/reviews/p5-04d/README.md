# P5-04d 专注首页

正式本地应用 `127.0.0.1:4180`，2026-09-19。

- 使用已验收 Ocean 弧线路径、64px 常规时钟、长时钟收缩、默认折叠可选字段；旧环形卡片不再渲染。
- 浏览器实测浅/深主题 × 320/390/430，document 宽度等于 viewport，无横向溢出；手动检查实际首页。浅色 [截图](focus-light.png)。摘要中的 17 秒是上批浏览器创建的合成会话，不是用户数据。
- 新增开始集成测试：90 分钟显示 `01:30:00`，默认折叠，展开选择分类，写入禁用输入和按钮，失败保留后重试仅创建一条 5400 秒记录。
- 类型、相关 ESLint、构建通过，保留既有构建废弃警告。未完成全应用 E2E/无障碍矩阵或物理设备检查，不宣称发布完成。
- Impeccable 手动参考用于表单层级、窄屏与状态审查，没有运行引擎。

下一步：健康完整历史与习惯/戒烟、今天和设置、快捷指令交付及全量上线门槛。专注剩余键盘/长文本/运行状态视觉纳入最终回归。
