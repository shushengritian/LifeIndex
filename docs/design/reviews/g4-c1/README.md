# G4 C1 专注验证

2026-09-19，本地合成内存原型；无真实数据、无部署、无真实 iPhone 验证。

## 已执行

- 空事项开始被拦截；填写事项、分类、备注后模拟开始失败，三项输入保留，重试可开始。
- 运行中切到今天可见继续专注入口，再回专注仍有结束/取消入口。
- 浏览器提前结束不足一秒未计入；秒级提前结束边界另由控制器断言覆盖。
- 使用明确标注的工具推进到期，同时模拟写入失败：显示 00:00 和待保存；重试后仅一条 25 分钟记录，全部统计 3/95 → 4/120。
- 编辑新记录事项与分类：总时长仍 120 分钟，分类汇总同步变化，起止和完成方式只读。
- 删除模拟失败时表单与记录保留；确认重试成功，总数/时长恢复 3/95。
- 今天过滤 2 次/75 分钟，全部 3 次/95 分钟，昨天样本未进入今日统计。
- 通过界面删除全部三条样本后，历史显示 0 次/0 秒与无记录提示；刷新恢复样本。加入本月筛选后再次核对 320px 无整页横向溢出。
- 自定义 240 分钟显示 04:00:00；带未提交时长进入历史触发丢弃确认。
- 390px 深浅主题截图；320/430px 历史页主内容无横向溢出。

## 可重复控制器检查

运行 `node docs/design/reviews/g4-c1/check-focus.cjs`。脚本加载真实原型控制器，用合成时钟与空 DOM 替身断言：时钟格式、59 秒保留、不足一秒、自然完成封顶、失败不追加、重试冻结终点、去重、取消不计数。不是 IndexedDB 或端到端持久化测试。

语法检查：`node --check docs/design/prototypes/g4-focus.js` 与 `g4-app.js`；`git diff --check`。

截图：[深色专注](dark-focus.png)、[深色历史](dark-history.png)、[浅色历史](light-history.png)。仅本地浏览器截图。

## 尚未验证

真正后台挂起/刷新恢复、系统时区变更、所有读屏/放大/键盘与浏览器组合仍待后续实现和 D；原型不持久化会话。戒烟 C2、分类管理未完成，不宣称全 G4 通过。
