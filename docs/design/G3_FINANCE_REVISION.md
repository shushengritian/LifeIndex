# G3 记账首页修订与 Impeccable 接入

> 最新审稿转到 [R3 石墨白](G3_FINANCE_R3.md)。本文件保留 R1/R2 历史，R2 尚未获用户认可。

日期：2026-09-16。状态：待用户审核，不是 G3 已通过，不授权 G4、生产实现或发布。

## 最新 R2：参考 MOZE 的精修

用户要求“继续打磨，参考 moze”，并补充“不必保留松绿主色”。当前预览已改为雾紫强调色及中性白/深灰底色；旧 R1 保存在 [上一版单页](prototypes/g3-finance-r1.html)。下文原 R1 的颜色、尺寸、导航限制和验证结果保留为历史，遇冲突以本节为准。

参考来源：2026-09-16 查阅 [MOZE 官方网站](https://moze.app/)和[官方产品主视觉](https://framerusercontent.com/images/DxdeEbvQ4Btma5eBw7khzfsCJE.png?height=2027&width=2752)。实际查看的是官方平板/概览组合图，不声称实测了 MOZE iPhone App。借鉴圆形日期焦点、分类色彩识别、右对齐金额和日历下方摘要的组织方式；未复制图标资产，也未引入账户、信用卡、预算、云同步或 AI 功能。雾紫方案是本次设计提案，不声称 MOZE 只有一种固定主题。

本轮 Impeccable 使用 polish 与 craft-floor 手动参考：保留信息结构、修正导航滚动边界、统一图标容器和强调状态。引擎/detect/钩子继续不运行。

### 视觉与交互增量

- 主色不再绑定松绿。浅色强调 `#7560B4`、选中底 `#EEE9F8`、正文 `#292A35`、辅助 `#6B6C7A`、汇总底 `#F5F5F9`；深色底 `#20212A`、强调 `#C3AFF1`、选中底 `#393047`、正文 `#F0EFF6`、辅助 `#B0B1C0`、汇总底 `#292A35`。
- 收入绿色仅表达财务语义，保留正负符号与收入/支出标签；支出不使用错误红色。
- 日历行高 48px，净额从 9px 提高到 10px；日期以圆形焦点加浅色单元格表示选中，月份居中、前后按钮分列。
- 分类恢复少量浅色图标容器（40px，圆角 13px），用餐暖杏、咖啡灰棕、交通灰蓝；它们用于快速识别，不恢复整页卡片套卡片。
- 月汇总采用连续浅底横区，数字保持小字号；行标题 15px、元信息 12px、金额 16px。
- 新增按钮为 44px 圆角方形；导航固定为应用容器的尾行，内容独立滚动，隐藏内容滚动条但保留键盘焦点与滚动能力。
- 新增“回到 9/16”原型参考日操作，离开参考日时出现，返回后焦点回到所选日。明确固定日期，不冒充系统今天；状态仍为内存演示。

### R2 验证

- 内置浏览器桌面深色截图及 390×844 浅色截图检查；320/390/430 × 双主题均无横向溢出，滚动区与底栏相接且不重叠。
- 默认三条记录末端位于应用内 y=770/773/773，底栏开始于 y=773，完整文字可见；额外尾部留白仍可滚动。更多记录或六周月份不保证首屏全显示。
- 日格宽度约 42.29/49.43/55.14px，高 48px；320px 宽下仍未达到 44px，保留触点限制，不声称全部达标。
- 选 9/5 后显示工资，回到 9/16 恢复三条；前往 10 月为空，可返回参考日。失败汇总为“—”，重试恢复；加载保留明确说明。
- 关键对比度：浅色辅助字/白底 5.18:1、辅助字/摘要底 4.77:1、白字/紫按钮 5.14:1；深色辅助字/页面 7.54:1、辅助字/摘要底 6.70:1、按钮前景/紫底 7.35:1。仅为选定颜色对，不是完整无障碍认证。
- 脚本解析检查通过；无 fetch/IndexedDB/localStorage/Service Worker 调用。仅隔离样板及设计/计划文档改动，不改生产代码、依赖或发布配置。

仍待验证：真机 safe-area/键盘、200% 字体与长内容全矩阵、屏幕阅读器完整路径。底栏仍是视觉标本，新增仍指向旧表单样板；这不是可投入日常使用的完整应用。

## 入口与范围

- [新版单页](prototypes/g3-finance-refined.html)
- [旧版三个样板](prototypes/g3-visual.html)，保留用于比较。
- 本机预览：`http://127.0.0.1:4176/g3-finance-refined.html`，服务运行时可用，不是手机线上地址。

用户“可以”承接的是审查/安装 Impeccable，先修订一个记账主页评估质感。此次不扩展健康、专注、表单或后台，不修改生产样式、数据库、依赖、部署配置。个人技能安装和项目 AGENTS 执行边界是本次工具配置变更，不是应用功能变更。

## 安装与安全边界

来源：`pbakaus/impeccable`，固定提交 `0a4e72a254f3b175c95b36b82e5f2e60fa63f116`，技能版本 4.3.1。通过官方 skill-installer 辅助脚本安装 `.agents/skills/impeccable` 到 `/Users/fh/.codex/skills/impeccable`；下轮可被 Codex 发现。

已检查技能入口、shell 启动器和 Codex hook 清单。启动器可能下载平台可执行程序及 SHA256 侧文件，校验后执行；这不等于完成二进制供应链审计。本次只复制技能包，没有运行其启动器、编译引擎、浏览器桥接脚本，也没有复制/注册仓库中的自动钩子。Windows 脚本与引擎未做完整安全审计。

按技能许可的 launcher refusal 回退，手动读取项目基线和 G3 设计资料；项目没有 PRODUCT.md / DESIGN.md，未伪造或改写它们。本次使用 Operate 布局原则与 craft-floor，保留 MOZE＋Things 已定方向，未开启新品牌概念流程。其 detect 命令未运行，不能宣称通过 Impeccable 自动审计。项目 AGENTS 已记录引擎/钩子需要另行授权。

## 布局评估与改动

旧版证据：独立卡片、列表图标底块和摘要块重复提供容器层级，页面视觉权重接近；数字虽可读，但分组主要依靠外框。新稿的主阅读路径不变：页标题/新增 → 月历 → 月汇总 → 所选日明细。

- 连续白色/炭绿色页面，仅用段落留白与细分隔组织信息；手机轮廓仅为桌面审稿背景，不是产品卡片。
- 日历 52px 行高，数字 15px；松绿色只强调选中日期、当前导航和新增入口。
- 月汇总 16px，320px 宽度为 14px；单位 11px，不使用巨型结余数字。
- 明细统一图标列、标题列、右对齐金额列；小数弱一级，分类采用同一套原创线性 SVG，无新依赖。
- 浅色背景 `#FFFFFF`、主文 `#23382E`、辅助 `#66736B`、强调 `#2B654C`；深色背景 `#19211D`、主文 `#E9EEE7`、辅助 `#A7B6AA`、强调 `#B4D5BA`。
- 不增加装饰插画、品牌字体或动画。中文系统字体适用于本次 Operate 模式，不追求营销页面的视觉表现。

## 原型交互与数据边界

月份可前后切换，选日联动列表；夹取日期避免跨月非法日号。仅 2026 年 9 月有合成账目。初始收入 8,500.00、支出 894.00、结余 7,606.00；16 日三笔共支出 76.00，从同一个整数分集合计算。

展示状态选择器仅为审稿工具：正常、整月空、加载、读取失败。加载/失败汇总为“—”，不能伪装成零；失败可重试。新增按钮显示前往既有表单样板的链接，不伪装已保存；不向既有样板传递日期，完整选日新增闭环仍以 G2 契约为准。底部五项是非交互视觉标本，不是已实现导航。

代码含关键状态注释和固定事件日志，不记录金额、时间、标题或其他个人记录。无网络请求、持久化或 Service Worker。

## 实测证据

环境：Codex 内置浏览器，合成数据。依照技能的有界检查方式，首轮检查后修正列表 HTML 闭合错误，再做一次确认，不持续微调。

| 检查 | 结果 |
| --- | --- |
| 320 / 390 / 430 × 浅 / 深 | 页面宽度与视口相同，无横向溢出；3 个明细行同宽，无错误嵌套 |
| 日格尺寸 | 42.29 / 49.43 / 55.14 × 52px；320px 时宽度仍未达到 44px，不能声称全部触点达标 |
| 选 9 月 5 日 | 明细显示工资 +8,500.00，月汇总未错误跟随日筛选变化 |
| 整月空 / 加载 | 空显示零汇总和记一笔；加载显示占位与“—” |
| 失败 → 重试 | 显示明确错误/重试；重试恢复示例数据 |
| 9 月 → 10 月 → 9 月 | 10 月显示真实的原型空状态，返回恢复合成记录 |
| 新增入口 | 明确提供既有表单样板链接，无写入或虚假成功 |
| 390 双主题截图 | 列表对齐修复，未再出现第二/三行异常换行 |
| 静态检查 | 脚本可解析；无 fetch、IndexedDB、localStorage、SW 调用 |

限制：不是生产 E2E；未运行 Impeccable detect、全量可访问性扫描、200% 字体或真实 iPhone 测试。9px 日历净额仅是索引，完整金额可通过选日及可访问名称读取，仍需用户确认易读性。导航位于内容尾部，390×844 截图未覆盖底栏；真实固定底栏、安全区和键盘避让留待完整原型验证，不能声称已具备原生体验。长内容矩阵及月份极端年份尚未验证。

## 审核问题

请先判断：这版连续页面、数字层级和去底块图标，是否比旧版更接近你希望的质感？

通过此单页方向后，仍需在 G3 同步健康与表单的视觉规则并审核，不能将一次单页认可视为全应用设计、实现或发布授权。
