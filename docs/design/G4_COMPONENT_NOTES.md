# G4 Today / Settings 组件记录

2026-09-19 图标库：42 个稳定 ID，8 组（常用、餐饮、交通出行、购物消费、居家缴费、健康宠物、学习娱乐、收入财务），沿用 24×24、1.7px 圆角线条与语义配色。编辑器只显示当前组，已选项跨组保留在预览和表单中；浏览分组不产生业务草稿变更。新增图形为内联 SVG，无依赖、无网络字体。见 [验证记录](reviews/g4-icons/README.md)。

2026-09-19 B1 补充：健康历史改为独立主内容页，复用 `record` 行与彩色图标；日期分组标题 13px、上距 26px、下距 4px。编辑沿用底部表单，危险删除放在表单内容底部并二次确认。保存替换原 ID、失败保留草稿；历史返回恢复健康首页滚动。具体证据见 [B1](reviews/g4-b1/README.md)，不改变既有语义颜色。

2026-09-19 · 批次 A · 手动源码提取。用户批准 Ocean 方向扩展到 G4；不是生产实现、发布授权或验收结论，不建立新的视觉世界。
依据：[G4 CSS](prototypes/g4-app.css)、[G4 HTML](prototypes/g4-app.html)、[Ocean CSS](prototypes/g3-ocean.css)、[Ocean tokens](G3_OCEAN_TOKENS.md)、[G4 进度](G4_PROGRESS.md)。HTML 只提供壳，实际页面结构补查 [G4 JS](prototypes/g4-app.js) 的 Today / Settings 渲染及事件分支。
按 Impeccable document 的手动提取方法记录现状；仅写本文，不生成根 DESIGN、sidecar 或配置，不运行 launcher/engine/hooks。本次未运行测试、浏览器或真机检查；独立评审另行记录。

## 继承的 Ocean 体系

- 字体：`-apple-system, BlinkMacSystemFont, "PingFang SC", sans-serif`，根 `15px/1.5`，数字 `tabular-nums`；页标题 `28px/700`、字距 `-.03em`。
- 深色 / 浅色依次为：背景 `--bg #101f35 / #f1f6fb`，表面 `--surface #1a2d45 / #fff`，抬高表面 `--raised #243b55 / #e5eef7`。
- 文字 `--text #edf4ff / #172c46`，辅助文字 `--muted #a9bdd4 / #516982`，分隔线 `--line #354c64 / #cfdeec`。
- 主色 `--accent #71d5ff / #126590`，主色上文字 `--on-accent #10243a / #fff`，柔和底色 `--soft #23445d / #dceefa`；边界 `--border #7890a9 / #788da4`。
- 分类沿用 `--orange / --blue / --purple / --green`，底块为对应色的 12% 混透明；危险文字沿用 `--danger`。完整双主题值以 Ocean tokens 为准，G4 CSS 未新增自定义属性。
- 共用图标 `23×23px`，小图标 `17×17px`；常规彩色底块 `39×39px`、圆角 `13px`。深浅主题保留相同信息层级。
- 常规内容靠色阶与分隔线组织，无新增卡片阴影；主按钮圆角 `14px`，次按钮圆角 `13px`、最小高度 `47px`、内边距 `10px 16px`。

## 共用布局与状态

- 纵向壳占满容器；模拟状态栏高 `46px`；主内容独立滚动，内边距 `6px 20px 22px`，宽度 ≤350px 时左右改为 `15px`。
- 底部五导航为今天、记账、专注、健康、设置，等宽五列、间距 `2px`；最小高度 `76px`，内边距 `8px 12px max(14px, env(safe-area-inset-bottom))`。
- 导航按钮最小高度 `50px`、圆角 `16px`、文字 `10px`；选中态用 `--soft / --accent` 和 `aria-current=page`。
- 控件焦点环 `3px --accent`、偏移 `3px`；禁用透明度 `.6`，按钮按下缩放 `.97`；继承减少动态效果规则。尺寸记录不代表触点或无障碍验收。

## 今天：新增布局与组件

- 顺序：标题与固定演示日期 → 记账主动作 → 专注横条 → 两项今日习惯 → 两列摘要 → 可选保存反馈 → 合成样本说明。
- 引导区内边距 `12px 0 18px`；标题 `23px/600`、字距 `-.025em`；说明 `13px`、外边距 `7px 0 20px`；全宽“记一笔”最小高度 `54px`。
- 专注横条复用设置行：最小高度 `70px`、间距 `12px`，覆盖内边距为 `14px`，表面色背景、圆角 `16px`、下外边距 `22px`；标题 `15px/600`，说明 `12px`、上间距 `3px`。
- 横条根据是否有会话显示“给自己一段专注时间”或“继续本次专注”，点击进入专注页；它本身不是开始计时按钮。
- 习惯是“阅读 20 分钟”“晨间喝水”，显示完成数 `/ 2`；复用普通记录行最小高度 `62px`、内边距 `10px 0`、间距 `11px`，不是健康页的 `57px` 习惯行。
- 习惯图标底块仍为 `39×39px`；勾选圆 `32×32px`、边界 `1.5px`，完成态用主色填充；整行按钮通过 `aria-pressed` 表达完成/撤销。
- 摘要两列 `1fr 1fr`、间距 `18px`、内边距 `22px 0`、上外边距 `16px`、顶部 `1px` 分隔线；标签与尾行 `12px`，数值 `23px/550`、外边距 `7px 0`，分钟单位 `13px`。
- 记账表单默认固定演示当天 `2026-09-18`，不改日历上下文；点击支出摘要才将日历切至该日。无账目时显示“尚无记录”；专注摘要显示分钟与次数。

## 设置：分组、详情与安全说明

- 四组依次为分类管理、外观、数据与安全、其他；组上外边距 `24px`，组标题 `12px/500`、下间距 `5px`。
- 设置行全宽、最小高度 `70px`、内边距 `12px 0`、间距 `12px`；标题 `15px/600`，说明 `12px`、上间距 `3px`；相邻行用 `1px --line` 分隔。
- 分类使用默认关闭的原生 `details`；summary 最小高度 `70px`、间距 `12px`，展开时箭头旋转 `90deg`。子项左缩进 `51px`、最小高度 `48px`、文字 `14px`。
- 分类子项为支出、收入、专注、运动分类；当前仅打开后续批次说明，不具备创建、改名或归档管理能力。
- 外观整行进入详情；另有导出备份、从备份恢复、关于 LifeIndex。详情头间距 `8px`、外边距 `8px 0 24px`，标题 `24px`，返回按钮最小 `44×44px`。
- 详情说明文字 `14px`、行高 `1.8`、下间距 `24px`；长标题、说明及摘要数值允许断行，箭头不收缩。
- 外观选项依次跟随系统、浅色、深色：行最小高度 `94px`、内边距 `12px 0`、间距 `13px`、底部 `1px` 分隔线，以 `aria-pressed` 和勾选圆表示选择。
- 主题缩略图 `44×58px`、边界 `1px`、圆角 `9px`、内边距 `10px 6px`；内部条高 `5px`、间距 `6px`，第二条宽 `65%`、透明度 `.5`。硬编码色均取自 Ocean，系统图为浅深各半的 90deg 渐变。
- 主题切换模拟等待 `250ms`，期间禁用当前页面按钮和导航；失败回退原主题并提示。设置只影响当前原型，刷新重置；跟随系统监听设备外观变化。
- 安全说明块使用表面色、圆角 `16px`、内边距 `18px`、间距 `12px`、下外边距 `24px`；标题 `15px`、下间距 `8px`，正文 `13px/1.8`。
- 导出步骤列表左内边距 `24px`、下外边距 `28px`，条目内边距 `9px 0`、文字 `14px`；恢复次按钮全宽、上间距 `12px`。
- 合成备份事实列表外边距 `20px 0`；行内边距 `13px 0`、间距 `12px`、文字 `13px`、底部分隔线。预览复用底部 dialog：上角 `24px`、最大高 `90dvh`、头尾吸附。
- 替换确认框继承宽 `calc(100% - 32px)`、最大宽 `380px`、圆角 `20px`、内边距 `23px`；确认仅结束演示，取消返回预览。
- 关于页标识区内边距 `24px 0 30px`；图标 `48×48px`、下间距 `12px`，名称 `27px`，副文 `13px`、上间距 `6px`。

## 数据边界与源码不一致

- 全部记录为内存合成样本，刷新重置；不选择或读取真实备份，不读写数据库、不替换本机数据、不生成或下载文件、不调起系统分享。
- 导出完成提示、无效文件提示、V3 备份事实和替换确认均为演示，不构成真实文件校验、迁移、恢复或已有备份的证明。
- 记录后复核修正：专注样本及完成记录带演示日期，Today 分钟与次数仅汇总演示当天；仍不代表真实跨午夜或后台验证。
- 记录后复核修正：Today 保存反馈改用 Ocean 已有的 `saved-notice`，沿用其柔和底色和回看入口布局。
- G3 tokens 中“今天/设置入口仅说明后续范围”描述的是保留的 G3 原型；G4 已新增上述页面。批次 B–D 仍待完成，本文不推进审核关卡。
# B2 补充（2026-09-19）

沿用 Ocean 双主题，无新字体、依赖或品牌色。习惯名称与 44px 打卡按钮拆分；详情使用两列数字定义列表、23px 名称、28px 统计数值。14 周热力图为 24px 日期格、1px 间距，实色/勾号/虚线区别完成与计划状态；窄屏图内滚动。图标为既有 SVG，编辑页代表性图标/颜色选择尚不是生产全集。目标与习惯表单继承底部 sheet、草稿保护和失败保留；暂停与清除均明确影响。统计、首页与热力图共用记录，禁止独立写死指标。证据与剩余验收见 [G4 进度](G4_PROGRESS.md) 和 [B2](reviews/g4-b2/README.md)。
# C1 专注补充（2026-09-19）

保留已批准的 Ocean 弧线、64px 计时与主按钮；自定义超过一小时显示 HH:MM:SS。分类/备注放入原生 details，摘要触区至少 44px；历史为独立页，四段日期筛选、分类汇总分隔行与带图标的完成记录。每条显示完成方式，秒级时长不舍入成零。详情 sheet 显示只读起止事实，仅编辑描述；沿用草稿与危险操作确认。失败待保存状态有显式重试，不以成功 toast 掩盖失败。颜色/字体/控件继承既有样板，未新增依赖或生产设计配置。详见 [C1 证据](reviews/g4-c1/README.md)。
# C2 戒烟补充（2026-09-19）

复用海蓝双主题、叶片图标、详情页头、底部 sheet 与风险确认。主动作是“截至现在未吸烟”，明确不代表全天；两个次按钮分别记录吸烟/烟瘾。7 日横排使用文字状态，不仅依赖颜色；月历沿用七列日期格，状态详情在下方。计划、烟瘾与吸烟表单沿用失败保留和脏草稿保护；统计不添加装饰性连续指标。固定时区与原型时间常显。设置“其他”提供隐藏后的恢复入口，不更改四组结构。原型/验收边界见 [C2 记录](reviews/g4-c2/README.md)。
# C3 分类补充（2026-09-19）

分类管理延续独立详情页、使用中/已归档分段、彩色 SVG 图标行；新增放右上角 44px 图标按钮。表单使用五列图标、四列颜色、实时预览和既有底部保存区。单选控件保留原生语义与键盘焦点，选中轮廓/勾号和颜色共同表达状态；仅选中色块显示勾号。引用数量来自同组样本，历史名称/图标以稳定 ID 读取。没有重画品牌或增加依赖。代表性图标/颜色集与 D 待验收范围见 [C3](reviews/g4-c3/README.md)。

# D1 交付状态补充（2026-09-19）

独立审稿页继承 Ocean 令牌，不增加产品导航。连接提示为 14px 圆角横条，更新和 Action 用分隔线组织、状态标题 17px/解释 14px；主次按钮最小 48px，320px 时上下排列。单色 SVG 连接图标附加斜线表达离线，不只依靠颜色。验证工具为原生 details，勾选框保留原生语义；状态 live region，提交结果/取消后移动焦点到状态。纯内存状态机只使用固定事件日志，CSP 禁止网络/表单提交。状态页面是 G2-T09/T10 的交互补充，不是实现 SW 和 URL Action 的技术方案；[D1 证据](reviews/g4-d/README.md)区分检查与待验收项。

# D2 焦点补充（2026-09-19）

`g4-accessibility.js` 为隔离原型的重绘焦点保护：仅修复源元素被移除后的 BODY 焦点，优先同标识控件、否则主内容；不打断现存焦点或打开的 dialog。对当前 editor→confirmation 嵌套结构，在顶层弹窗首尾显式循环 Tab，保持中间原生控件操作。沿用 Ocean 的焦点轮廓，无新视觉令牌。生产实现需按 React 组件生命周期建立等价契约，不照搬全局 MutationObserver；[D2 证据](reviews/g4-d2/README.md)保留原生200%及完整读屏未验证边界。
