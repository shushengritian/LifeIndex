# Ocean：iPhone 验收

当前线上基线：2.2.0/V4，2026-09-20，源码 `3f2f670`，Pages 35453823388 成功。本地后续修订尚未发布；测试时必须记录实际版本。

## 开始前

记录设备、iOS 版本、使用入口（Safari 或主屏幕），在“设置 → 其他 → 关于 LifeIndex”核对版本。不要求先进行 Mac 测试。

| 项目 | 操作与通过标准 | 失败处理 |
| --- | --- | --- |
| 分类 | 一级点击进入二级；二级仅名称；创建/编辑/排序/归档后记账选择一致 | 不清数据，记录页面与步骤 |
| 记账 | 日历选日、手动新增/编辑/删除；今天“记一笔”保存和取消返回来源 | 检查记录是否实际写入，避免重复提交 |
| 系统控件 | 日期/时间、软键盘、安全区、滚动与弹层均可操作 | 记录设备与输入方式 |
| PWA 与离线 | 主屏幕启动；联网一次后断网新增，恢复连接和重启后记录仍存在 | 不用清缓存或清库掩盖问题 |
| 专注 | 后台或锁屏后返回，时间符合真实流逝 | 保留失败状态并记录时间线 |
| 备份 | 导出到 Files 并确认文件存在；只用隔离合成数据验证恢复 | 真实记录未经明确授权不得覆盖 |
| 更新 | 有草稿/保存中禁止更新；处理完成后明确确认更新 | 保留旧客户端，不强制刷新 |
| 无障碍 | 200% 放大、VoiceOver、焦点顺序、主要按钮可达 | 未测不得标通过 |

## 结果模板

- 实际版本 / 候选提交：
- iOS / 设备 / 入口：
- 日期与测试项目：
- 操作及结果：通过 / 失败 / 未测。
- 截图只用合成数据，不提交个人记录或完整备份。

自动化不代表真机验收；每项以用户实际操作和反馈为准。
