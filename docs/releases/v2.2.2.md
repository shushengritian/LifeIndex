# LifeIndex 2.2.2

日期：2026-09-20。用户明确授权发布后手机复验。当前：已部署，线上验证通过；手机原始故障待复验。

发布源码：`a1c616c5aad1c44c4e77092f768bf1f40c673216`。
[Pages 35486063644](https://github.com/shushengritian/LifeIndex/actions/runs/35486063644)：验证构建、部署、线上 smoke 三阶段全部成功。

## 内容

- 原创 Ocean App 图标及五种安装图标尺寸。
- 60 个分类图标、11 组，补齐旅游住宿、家庭教育与生活服务。
- 设置行、分类管理入口及外观选项圆角高亮。
- 限制设置/导航链接长按系统预览；弹层还原焦点不主动滚动。

数据库/备份仍为 V4，不清数据、不迁移已有记录。含新增图标的备份须用本版或更新版本恢复，不能降级到旧图标枚举代码。

## 验证

发布前实现验证：207 项单元/集成通过；完整 Chromium/WebKit 73 通过、1 原有跳过；最后样式增量两引擎 10 项通过。

本次流水线再次通过静态、207 项单元/集成、73 项浏览器（1 原有跳过）、Pages 子路径构建与线上 smoke。构建 job `106012550281`，部署 job `106013527452`，线上 job `106013555053`。

独立隔离浏览器访问真实 HTTPS 网站：13 通过、1 原有 WebKit 离线全页重载跳过；确认实际版本 2.2.2/V4、安装资源、导航、本地持久化与离线写入。线上 `icon-512.png` SHA-256 与本地发布文件一致：`cc8047dd90416286ee264f4a1ec5a0d5d6df74187a4d6b9ec2fefb239baa697e`。没有访问或改写用户手机数据库。

## 手机复验（待用户）

联网打开原 App，完成应用内更新，在“设置 → 关于”确认 **2.2.2**。不卸载、不清网站数据。

1. 长按“从备份恢复”，确认不再弹出链接预览菜单。
2. 在该位置上下拖动，再点空白区域；确认底部五个入口未下移、消失。
3. 点击记账、健康再回设置，确认导航可用。
4. 正常点击“从备份恢复”仍能进入；选择文件后取消，不实际恢复数据，检查底栏。

真实 iOS 系统弹框尚未复验；不能把浏览器自动化记为原始故障已彻底修复。主屏幕图标可能由 iOS 独立缓存，不为更换图标删除 App。
