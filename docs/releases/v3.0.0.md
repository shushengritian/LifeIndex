# LifeIndex 3.0.0 — 可用产品里程碑

2026-09-20：用户要求完成当前体验优化后发布 3.0.0。当前：已部署，线上验证通过；手机专项待用户确认。

发布源码 `78b86135369281c15848c1dce0af6f8d01c86d98`，[Pages 35488041361](https://github.com/shushengritian/LifeIndex/actions/runs/35488041361) 验证构建、部署、线上 smoke 全部成功。

## 交付范围

- 保留 Ocean 风格、今天/健康/专注/记账/设置五入口；不扩展为复杂系统。
- 专注原先的固定“0 分钟”起点改成 HTML 的实时“已专注 MM:SS”，目标时长单独显示。剩余倒计时、进度弧和已用时间来自同一时间戳投影；离页/后台返回不依赖回调次数计时。
- 今日/本周仍只统计完成且保存的会话，增加明确说明；取消不会计入累计。
- 专注按钮到汇总分隔线 28px，汇总说明到历史入口 20px；健康区块分隔内距 20px，记账统计展开行垂直内距 12px。
- 应用版本升到 3.0.0，数据库与备份仍为 V4，无破坏性迁移，无新增依赖。

## 验证与手机复验

本地类型、lint、格式、生产构建与 diff 检查通过；45 文件/208 项单元集成通过；Chromium/模拟 iPhone WebKit 77 项通过、1 原有离线全页重载跳过。新测试验证跨分钟实时标签/进度、离页返回、提前结束统计、窄屏留白。视觉证据见 [验证记录](../design/reviews/v3-focus/README.md)。

云端同样 208 单元集成、77 浏览器通过、1 原有跳过；构建 job `106017902759`、部署 `106018998776`、线上 smoke `106019028889` 全部成功。独立 HTTPS 线上复验 13 通过、1 原有跳过，核对实际 3.0.0/V4、资源、导航、数据持久化和离线写入。自动化使用隔离合成数据，没有修改用户手机记录。

手机更新后核对关于页面为 3.0.0。开始专注超过一分钟：已专注增加、中央剩余减少；结束保存后累计才增加。检查按钮与横线不再贴合。先前长按恢复链接导致底栏消失的真机复验仍未收到结果，不记为已通过。

不卸载、不清站点数据。新增图标的 V4 备份仍须由 2.2.2 或更新代码恢复，不支持退回旧枚举版本。
