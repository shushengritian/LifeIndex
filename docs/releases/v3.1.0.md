# LifeIndex 3.1.0 — 全应用操作体验更新

2026-09-21。用户授权见 ADR-0021。已发布，云端构建、部署与线上验证均成功；真机专项待用户确认。

发布源码 `418e233ba5a59897e231e30b2a2ff24949efe804`，[Pages 35559397107](https://github.com/shushengritian/LifeIndex/actions/runs/35559397107)。构建 job `106209229441`、部署 `106210661442`、线上 smoke `106210696949` 全部成功。

## 范围

- 保留 Ocean 双主题与今天、健康、专注、记账、设置五入口。
- 统一编辑器：明确关闭入口、固定头尾与中段滚动，草稿确认、写入锁及失败保留。
- 记账详情内删除；新增只读独立报表，分类条形图与每日趋势，空月份不伪造数据，返回保留日历上下文。
- 专注开始/结束图标主操作位于计时区；自然到时显示待保存时长，保存后才计入累计。
- 今天的习惯详情和打卡独立操作；返回恢复入口上下文，异日记账提供保存回执与查看记录。
- 健康、习惯、戒烟与设置编辑流程接入统一容器；一级分类保留图标，二级分类仅名称。
- 页面与弹层隐藏网页滚动条，保留触摸、滚轮与键盘滚动。系统长按菜单与文件选择器不是网页样式可控范围。

## 数据与更新

数据库/备份仍为 V4；无数据迁移、后台、账户、云同步或新增依赖。已有本地记录不清空，不要求卸载或清网站数据。扩展图标备份仍须用 2.2.2 或更新版本恢复。

## 验证

本地 49 文件 / 267 项单元集成通过；整版 Chromium/WebKit 83 通过、1 项原有 WebKit 离线全页重载跳过。格式、ESLint、TypeScript 与生产构建通过。最终冻结后重新构建并复验专注、报表、设置恢复与滚动条 18 项全部通过。浏览器隔离合成数据，不操作手机记录；不将模拟 iPhone 的结果等同真机验收。

云端同样 267 单元集成、83 浏览器通过/1 原有跳过；线上 smoke 13 通过/1 原有跳过。独立 HTTPS 线上复验同样 13 通过/1 跳过，无重试，验证实际 3.1.0/V4、资源、导航、健康/戒烟记录、离线写入与 Chromium 离线重载。桌面浏览器已有 2.2.0 缓存也显示更新提示，点击“立即更新”后关于页面为 3.1.0/V4；未清缓存或数据，但此观察不替代 iPhone 更新验证。135 份 Markdown 的 610 个相对文件引用均存在（行号和查询串归一后检查）。

本机 pnpm 包装器试图自动安装并因 no-TTY 停止，未执行重装或改锁文件，改用已安装固定版本的工具执行同等质量门槛。CI 仍按原 `pnpm quality` 流程执行。构建保留已有 Workbox `inlineDynamicImports` 弃用警告，不影响成功结果。

独立代码审核：设置/备份未发现阻断问题；共享容器、报表与今天未发现额外阻断。专注开始草稿在后台读取失败时卸载的问题已修复，新增 dirty/busy 保留与失败后重试测试；详情错误补焦点。实现见 OPERATION_FOCUS_IMPL / OPERATION_HEALTH_IMPL / OPERATION_SETTINGS_IMPL。

## 发布后真机清单（待用户执行）

1. 联网从原桌面图标打开，处理应用更新提示，在关于页面核对 3.1.0。
2. 深浅主题各检查一次：页面、报表和长编辑器正常滚动，右侧不出现网页滚动条，保存/关闭不被键盘遮挡。
3. 记账新增、编辑与删除确认；切换报表月份/收支并返回；今天异日记账查看保存日期。
4. 专注开始超过一分钟，离页及锁屏后返回，再结束保存；检查时间与累计，不重复保存。
5. 健康各表单取消草稿、重新打开、保存；今天习惯详情返回及打卡。
6. 设置长按恢复入口→拖动→点击空白→取消系统框，底栏仍可见可点；不为验证而执行真实恢复。
7. 初次联网加载后，断网重开并记录一条合成数据，恢复联网后检查仍存在。

真机未测项保持待验证；如有问题反馈页面、步骤、主题和截图，不附个人备份内容。
