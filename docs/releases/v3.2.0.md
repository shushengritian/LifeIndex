# LifeIndex 3.2.0 — 内容查看与图标操作

2026-09-21。用户授权开发和部署见ADR-0022。当前发布候选，部署尚待完成；不能将候选验证写成线上已更新。

## 本次变化

- 今日账目与专注改为单列内容入口，标题仅保留新增或播放；查看和写入不同区域，习惯名称/打卡仍独立。
- 记账报表移至日历汇总下方独立内容行；关闭/居中标题/对勾保存为固定顶部工具栏。保持分类、日期备注、跨日回执与真实存储。
- 健康体重与运动点内容查看真实历史，加号只新增。戒烟未建计划整卡创建，已有计划点内容查看、加号选事件；习惯独立详情与打卡。
- 健康各编辑器图标保存，破坏性确认保留明确文字；输入失败保留、忙碌锁与草稿退出确认不变。
- 专注准备/沉浸计时分离，始终完整360°轨道，收起不暂停。停止面板区分继续、保存结束和放弃；自然到时持久化与失败重试仍采用原有可靠逻辑。
- 保留Ocean双主题和隐藏网页滚动指示器；内容可滚动，不通过锁死页面掩盖溢出。

## 数据与发布方式

数据库与完整备份V4不变，无迁移、无依赖升级。保留本地记录；不卸载、不清网站数据。本地用户独立AGENTS.md改动不包含在发布提交中。

## 验证记录

- 52文件275项单元/集成通过，含圆环0/50/100%、收起重入、真实保存失败重试、form关联保存及查看不写入。
- 原型5组DOM检查通过；原型仅设计参考，不用于生产数据。
- 正式浏览器定点12项通过（完整圆环、320/390/430宽度、入口间距、双主题）。整版首跑89通过、1原有WebKit离线重载跳过、2项旧路径失败：测试在沉浸计时期间直接找底栏，而新交互要求先收起。更新测试走真实收起路径后，相关跨模块与新增健康布局6项全通过；总覆盖91通过/1跳过，不是声称首跑零失败。
- 类型、ESLint、格式、生产构建与diff检查通过；Today/Finance/Sheet/global.css独立源码审核未发现阻断。健康7文件25项窄测试通过。数据层、SW及依赖锁文件无改动。
- 已核对正式圆环，以及浅色WebKit/深色Chromium健康、今日和编辑器截图。浏览器自动化不证明真实iPhone后台、安全区或键盘行为。
- 本机通过应用更新提示从旧缓存载入新界面，未清缓存/数据。检查正式圆环、今日与记账编辑器截图；该观察不等于iPhone验证。

冻结源码`48663c6`完整复跑：91通过、1原有跳过，无失败。发布验证脚本预跑发现戒烟旧入口定位未同步，已改为创建计划及显式加号选择事件；该修订只更新测试，不改变应用代码。首个云端流水线由修订后的提交替代，最终结果另行记录。

## 手机复验（待用户）

1. 联网从原桌面图标打开，按提示更新，在设置→关于确认3.2.0；不要卸载或清数据。
2. 今日点摘要应查看，点加号应新增；健康体重/运动同样验证；习惯点名称不得打卡。
3. 专注开始后检查完整圆环；收起再重入，锁屏后回来核对时间；提前结束保存一次，汇总不重复。
4. 记账和健康顶部✓保存；关闭修改过的草稿时先确认，继续填写保留内容。检查软键盘不会挡住头部动作。
5. 深浅主题、长内容滚动及设置长按系统菜单取消后，底栏仍可用、网页右侧无滚动条。沉浸专注期间底栏按设计隐藏，收起后恢复。

真实iPhone、系统菜单、系统分享、后台挂起、VoiceOver及主屏幕PWA更新仍待设备确认。
